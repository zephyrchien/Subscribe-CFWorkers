{
	"private": true,
	"name": "subscribe",
	"version": "1.0.0",
	"description": "A template for kick starting a Cloudflare Workers project",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "prettier --use-tabs --semi --write '**/*.{js,css,json,md}'"
	},
	"author": "zephyr <i@zephyr.moe>",
	"license": "MIT",
	"devDependencies": {
		"prettier": "^1.19.1"
	},
	"dependencies": {
		"uuid": "^8.3.2"
	}
}
